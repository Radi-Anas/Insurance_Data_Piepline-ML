# Production Environment Configuration
ENV=production

# Database (use actual production credentials from secrets manager)
DB_HOST=prod-db.example.com
DB_PORT=5432
DB_NAME=insurance_fraud_prod
DB_USER=prod_user
DB_PASSWORD=SET_FROM_ENVIRONMENT_VARIABLE
